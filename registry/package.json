{
  "name": "@andromeda/registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Andromeda registry — multi-seller catalog, transaction ledger, peer-review escrow.",
  "scripts": {
    "dev": "node --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types test/registry.test.ts"
  },
  "dependencies": {
    "@andromeda/core": "*",
    "better-sqlite3": "^12.9.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "typescript": "^5.6.3"
  }
}
