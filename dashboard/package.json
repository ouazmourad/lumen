{
  "name": "@andromeda/dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Andromeda local dashboard. Phase 3 ships the headless control plane (in MCP) + this CLI; the Tauri GUI shell is deferred per ADR 0006.",
  "scripts": {
    "dev": "node src/control-plane-cli.js",
    "cli": "node src/control-plane-cli.js",
    "build": "node src/tauri-build-stub.js",
    "tauri:build": "node src/tauri-build-stub.js"
  }
}
