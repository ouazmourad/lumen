# Andromeda registry config

# DB path (default: <repo>/registry.db)
# ANDROMEDA_REGISTRY_DB_PATH=

# Admin secret for /v1/platform/* endpoints (Phase 6)
# ADMIN_SECRET=change-me

# Mock mode (registry has no wallet of its own; this only affects test-mode features)
MOCK_MODE=true
